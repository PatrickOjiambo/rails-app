<div class="max-w-2xl mx-auto p-8 bg-white rounded-lg shadow-md">
  <h1 class="text-3xl font-bold text-gray-800 mb-4">Request Status</h1>
  
  <div class="mb-6 p-4 bg-blue-50 border-l-4 border-blue-500 rounded">
    <p class="text-gray-700">
      Your request for a 
      <span class="font-semibold text-blue-600"><%= @website_request.business_type %></span> 
      website is: 
      <strong class="<%= status_color(@website_request.status) %>">
        <%= @website_request.status.humanize %>
      </strong>
    </p>
  </div>

  <% if @website_request.complete? && @website_request.generated_website.present? %>
    <div class="bg-green-50 border-l-4 border-green-500 p-4 rounded mb-4">
      <p class="text-green-800 font-medium mb-3">Your website is ready!</p>
      <%= link_to "Preview Your Website", 
          generated_website_path(@website_request.generated_website.subdomain), 
          target: "_blank",
          class: "inline-block bg-green-600 hover:bg-green-700 text-white font-medium py-2 px-4 rounded transition duration-200" %>
    </div>
  <% else %>
    <div class="bg-yellow-50 border-l-4 border-yellow-500 p-4 rounded">
      <p class="text-yellow-800">
        We're hard at work building your site. We'll email you at 
        <span class="font-medium"><%= @website_request.email %></span> 
        as soon as it's done.
      </p>
    </div>
  <% end %>
</div>
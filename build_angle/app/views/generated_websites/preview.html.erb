<!DOCTYPE html>
<html>
<head>
  <title>Preview: <%= @page.title %></title>
  <style>
    body { margin: 0; }
    .preview-nav { background-color: #222; padding: 10px; text-align: center; }
    .preview-nav a { color: white; text-decoration: none; margin: 0 15px; }
    .preview-content { border: 0; width: 100%; height: 90vh; }
  </style>
</head>
<body>
  <nav class="preview-nav">
    <% @website.generated_pages.order(:page_type).each do |p| %>
      <%= link_to p.title, generated_website_page_path(@website.subdomain, p.page_type) %>
    <% end %>
  </nav>

  <div class="preview-content">
    <%= @page.content.html_safe %>
  </div>
</body>
</html>